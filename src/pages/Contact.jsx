import{useState}from"react";import api from"../api/axios.js";export default function Contact(){const[f,s]=useState({name:"",email:"",phone:"",subject:"",message:""}),[m,sm]=useState("");const go=async e=>{e.preventDefault();await api.post("/enquiries",f);sm("Enquiry sent successfully")};return <form onSubmit={go} className="card mx-auto max-w-xl space-y-3"><h1 className="text-2xl font-bold">Contact</h1>{m&&<p className="text-green-600">{m}</p>}{Object.keys(f).map(k=>k==="message"?<textarea key={k} className="input" placeholder={k} onChange={e=>s({...f,[k]:e.target.value})}/>:<input key={k} className="input" type={k==="email"?"email":"text"} placeholder={k} onChange={e=>s({...f,[k]:e.target.value})}/>) }<button className="btn">Send</button></form>}
